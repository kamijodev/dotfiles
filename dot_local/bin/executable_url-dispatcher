#!/bin/bash
# URL Dispatcher - URLのドメインに応じて適切なアプリで開く

URL="$1"
LOTION_DEBUG_PORT=19222

open_in_lotion() {
  local url="$1"

  # Lotionが既にリモートデバッグ付きで起動中か確認
  if curl -s --max-time 1 "http://127.0.0.1:$LOTION_DEBUG_PORT/json/version" > /dev/null 2>&1; then
    # tab-barページのWebSocket URLを取得
    local tab_bar_ws
    tab_bar_ws=$(curl -s --max-time 2 "http://127.0.0.1:$LOTION_DEBUG_PORT/json/list" | python3 -c "
import sys, json
for t in json.load(sys.stdin):
    if 'tab-bar' in t.get('url', ''):
        print(t['webSocketDebuggerUrl'])
        break
" 2>/dev/null)

    if [ -n "$tab_bar_ws" ]; then
      # LotionのtabBarAPI経由で新規タブを開く
      local escaped_url
      escaped_url=$(printf '%s' "$url" | sed 's/\\/\\\\/g; s/"/\\"/g')
      echo "{\"id\":1,\"method\":\"Runtime.evaluate\",\"params\":{\"expression\":\"tabBarAPI.createTab({url: \\\"$escaped_url\\\"})\",\"awaitPromise\":true}}" \
        | websocat -t --no-close -1 "$tab_bar_ws" 2>/dev/null

      # niriでLotionウィンドウにフォーカス
      local win_id
      win_id=$(niri msg --json windows 2>/dev/null | python3 -c "
import sys, json
windows = json.load(sys.stdin)
lotion = [w for w in windows if w.get('app_id') == 'Lotion']
if lotion:
    print(lotion[0]['id'])
" 2>/dev/null)

      if [ -n "$win_id" ]; then
        niri msg action focus-window --id "$win_id" 2>/dev/null
      fi
      return 0
    fi
  fi

  # Lotionが起動していない or CDP接続失敗 → 新規起動
  exec /usr/bin/lotion --remote-debugging-port=$LOTION_DEBUG_PORT "$url"
}

open_in_slack() {
  local url="$1"
  local SLACK_CONFIG="$HOME/.config/Slack/storage/root-state.json"

  # ドメインからチームIDを解決
  resolve_team_id() {
    local domain="$1"
    python3 -c "
import json, sys
with open('$SLACK_CONFIG') as f:
    data = json.load(f)
for tid, ws in data.get('workspaces', {}).items():
    if ws.get('domain') == sys.argv[1]:
        print(tid)
        break
" "$domain" 2>/dev/null
  }

  local deep_link

  # https://WORKSPACE.slack.com/archives/CHANNEL/pTIMESTAMP
  if [[ "$url" =~ ^https://([^.]+)\.slack\.com/archives/([^/]+)(/p([0-9]+))? ]]; then
    local domain="${BASH_REMATCH[1]}"
    local channel="${BASH_REMATCH[2]}"
    local ts_raw="${BASH_REMATCH[4]}"
    local team_id
    team_id=$(resolve_team_id "$domain")

    if [ -n "$team_id" ]; then
      deep_link="slack://channel?team=${team_id}&id=${channel}"
      if [ -n "$ts_raw" ]; then
        # p1771472545064259 → 1771472545.064259
        local ts="${ts_raw:0:10}.${ts_raw:10}"
        deep_link="${deep_link}&message=${ts}"
      fi
    fi

  # https://app.slack.com/client/TEAM/CHANNEL
  elif [[ "$url" =~ ^https://(app\.)?slack\.com/(client|huddle)/([^/]+)/([^/?]+) ]]; then
    deep_link="slack://channel?team=${BASH_REMATCH[3]}&id=${BASH_REMATCH[4]}"
  fi

  # フォールバック
  if [ -z "$deep_link" ]; then
    deep_link="slack://open"
  fi

  exec /usr/bin/slack -- "$deep_link"
}

case "$URL" in
  https://www.notion.so/* | https://notion.so/*)
    open_in_lotion "$URL"
    ;;
  https://app.slack.com/* | https://*.slack.com/*)
    open_in_slack "$URL"
    ;;
  *)
    exec /usr/bin/google-chrome-canary "$URL"
    ;;
esac
