monitor.alsa.rules = [
  {
    matches = [
      { node.name = "~alsa_input.*", node.name = "!~alsa_input.*Fifine*" }
    ]
    actions = {
      update-props = {
        node.disabled = true
      }
    }
  }
]
